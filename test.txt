<?php
session_start();

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = $_POST['username'];
    $password = $_POST['password'];

    // Example check for login
    if ($username === 'admin' && $password === 'password') {
        $_SESSION['success'] = "Welcome back, $username!";
    } else {
        $_SESSION['error'] = "Invalid credentials!";
    }
}

// Display toastr notifications based on session
if (isset($_SESSION['success'])) {
    echo "<script>toastr.success('" . $_SESSION['success'] . "');</script>";
    unset($_SESSION['success']);
}

if (isset($_SESSION['error'])) {
    echo "<script>toastr.error('" . $_SESSION['error'] . "');</script>";
    unset($_SESSION['error']);
}
?>


<!-- home.php -->
<?php

include '../helper/StackManager.php'; 
$title = "Page one";
ob_start(); // Output buffering শুরু করুন
?>
<h2>page one</h2>

<?php
$content = ob_get_clean(); // Output buffering থেকে কনটেন্ট নিন

// Push CSS
StackManager::push('styles', '<link rel="stylesheet" href="styles.css">');

// Push JavaScript
StackManager::push('scripts', '<script> alert("I\'m coming from child"); </script>');

include 'layout.php'; // layout.php ফাইলটি ইমপোর্ট করুন

?>


{
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Config\\": "config/"
        },
        "files": [
        "config/pathupdate.php"
        ]
    }
}




